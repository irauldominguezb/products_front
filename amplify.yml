version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g flutter
        - flutter doctor
    build:
      commands:
        - flutter build web
  artifacts:
    baseDirectory: build/web
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
